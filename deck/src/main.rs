use std::fs;
use rand::seq::SliceRandom
use rand::thread_rng

struct Deck {
    cards: Vec<String>
}

impl Deck {
    fn new() -> Self {
        let suits = vect["Spades", "Diamonds", "Hearts", "Clubs"]
    }
}